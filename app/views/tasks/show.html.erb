<ul>
  <li><%= @task.title %></li>
  <li><%= @task.body %></li>
</ul>  
<p>選択したカテゴリー</p>
<ul>
  <% @task.categories.each do |category| %>
    <li><%= category.title %></li>
  <% end %>
</ul>  
<%= link_to '編集', edit_task_path %>
<%= button_to '削除', task_path, method: :delete %>
